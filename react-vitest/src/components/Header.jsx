const Header = ({ title }) => {
  return (
    <>
      <h1>{title}</h1>
      <button>first button</button>
      <button>second button</button>

      <span title="999">999 $</span>
    </>
  );
};

export default Header;
